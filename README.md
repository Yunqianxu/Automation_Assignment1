Run 'terraform init' to initate the modules<br />
Modify the ssh key pairs in "linux_vm_module", the attributs names are pulic_key and private_key<br />
Run 'terraform apply' to provision the infrastures.<br />
